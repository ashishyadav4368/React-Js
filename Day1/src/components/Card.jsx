import React from "react";

// const Card = (props) => {
//   return <div>{props.children}</div>;
// };

// export default Card;

const Card = ({ children }) => {
  return <div>{children}</div>;
};

export default Card;

//here we have destructure the children , the output for both the snippets will be same
